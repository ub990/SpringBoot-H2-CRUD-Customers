# SpringBoot-H2-CRUD-Customers

Simple Customer CRUD using Springboot, H2 and JPA.
A simple CRUD system using Springboot, H2, Hibernate and JPA to display an application that suplies control for a customer operations.
